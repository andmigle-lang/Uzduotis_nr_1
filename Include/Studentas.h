#pragma once
#include <string>
#include <vector>
#include <list>

using std::string;
using std::vector;
using std::list;

struct Studentas {
    string var;
    string pav;
    vector <int> paz;
    int egz;
    double gal;
    double med;
};
